{
  "name": "git-auto-merge",
  "version": "0.0.26",
  "description": "git auto push and merge",
  "main": "src/index.js",
  "bin": {
    "git-auto-merge": "./bin/git-auto-merge.js",
    "git-auto-push": "./bin/git-auto-push.js",
    "git-auto-init": "./bin/git-auto-init.js"
  },
  "scripts": {
    "gm": "node ./bin/git-auto-merge.js",
    "gp": "node ./bin/git-auto-push.js",
    "deploy": "rm -rf dist && tsc && node ./scripts/publish.js",
    "rebuild": "rm -rf dist && tsc"
  },
  "engines": {
    "node": ">=10.0.0"
  },
  "files": [
    "bin",
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/heny/git-auto-merge.git"
  },
  "keywords": [
    "git"
  ],
  "author": "heny",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/heny/git-auto-merge/issues"
  },
  "homepage": "https://github.com/heny/git-auto-merge#readme",
  "dependencies": {
    "dayjs": "^1.10.7",
    "inquirer": "^8.2.0",
    "os-locale": "5.0.0",
    "shelljs": "^0.8.4"
  },
  "devDependencies": {
    "@types/inquirer": "^8.1.3",
    "@types/shelljs": "^0.8.10",
    "typescript": "^4.5.4"
  }
}